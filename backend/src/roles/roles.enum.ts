// Role IDs aligned with production user_types table
export enum RoleEnum {
  "fitter" = 1, // Production UserTypeID 1
  "admin" = 2, // Production UserTypeID 2
  "factory" = 3, // Production UserTypeID 3
  "customsaddler" = 4, // Production UserTypeID 4
  "supervisor" = 5, // Extended role (not in legacy)
  "user" = 6, // Extended role (not in legacy)
}
